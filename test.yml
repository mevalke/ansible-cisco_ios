---
- name: Create one or multiple VLANs 
  hosts: all
  vars_files: vars.yml
  gather_facts: false
  connection: local
  tasks:
  - name: Create VLAN
    cisco.ios.ios_config:
      lines:
      - name {{ item.name }}
      parents:
      - vlan {{ item.id }}
    loop: "{{ global_vlans }}"

